# Oracle 19c 소프트웨어 설치

### 0. GUI 설치 준비(X11)

Oracle Installer 같은 GUI 프로그램을 원격(X11)으로 띄우기 위해 X11 인증 파일을 확인한다.

```bash
login as: root
root@192.168.56.210's password:
Last login: Thu May  9 17:14:31 2024
/usr/bin/xauth:  file /root/.Xauthority does not exist

[root@ora19c ~]# xauth
Using authority file /root/.Xauthority
xauth> info
Authority file:       /root/.Xauthority
Number of entries:    1
xauth> exit

[root@ora19c ~]# ls -al /root/.Xauthority
-rw-------. 1 root root 52 May  9 17:18 /root/.Xauthority
```

<img width="1878" height="1001" alt="image" src="https://github.com/user-attachments/assets/84cfca2c-e22f-48f7-a055-e201852181a4" />

&nbsp;

**X11 동작 테스트(xclock)**

```bash
[root@localhost ~]# echo $DISPLAY
localhost:10.0

[root@localhost ~]# xclock
bash: xclock: 명령을 찾을 수 없습니다..

[root@localhost ~]# yum install -y xorg-x11-apps
```

<img width="1862" height="1027" alt="image" src="https://github.com/user-attachments/assets/9d5f032d-fd8d-43ea-bfe4-de5a822927dd" />

---

### 1. OS 버전 확인

Oracle 설치 전 OS 버전과 커널 버전을 확인한다.

```bash
[root@localhost ~]# rpm -qa *-release
oraclelinux-release-7.9-1.0.9.el7.x86_64

[root@localhost ~]# uname -a
Linux localhost.localdomain 5.4.17-2102.201.3.el7uek.x86_64 ...
```

| 리눅스 버전 | 권장 Oracle DB |
|-----------|--------------|
| OL 9 / RHEL 9 | 19c (19.19+), 23c |
| OL 8 / RHEL 8 | 19c, 21c |
| OL 7 / RHEL 7 | 12cR2, 18c, 19c |
| OL 6 / RHEL 6 | 11gR2, 12cR1 |

---

### 2. 네트워크 및 호스트 설정

/etc/hosts에 서버 IP와 호스트명을 등록한다.

```bash
vi /etc/hosts
```

```
127.0.0.1   localhost localhost.localdomain
192.168.13.181 ora19c
```

호스트명을 설정한다.

```bash
hostnamectl set-hostname ora19c
hostnamectl status
```

---

### 3. oracle-database-preinstall-19c 패키지 설치

preinstall 패키지를 설치하면 Oracle 설치에 필요한 사전 준비가 자동으로 완료된다.

```bash
yum -y install oracle-database-preinstall-19c
```

preinstall 패키지가 자동으로 처리하는 항목은 아래와 같다.

| 항목 | 내용 |
|------|------|
| 필수 패키지 설치 | glibc, gcc, libaio 등 |
| 커널 파라미터 설정 | /etc/sysctl.conf 자동 구성 |
| oracle 유저/그룹 생성 | oinstall, dba 등 |
| limits 설정 | /etc/security/limits.d/ 자동 구성 |

---

### 4. oracle 유저 그룹 확인 및 설정

그룹 생성 여부를 확인한다.

```bash
cat /etc/group | grep -E 'oinstall|dba|oper'
```

oracle 유저의 기본 그룹과 보조 그룹을 설정한다.

```bash
# 기본 그룹 설정
usermod -g oinstall oracle

# 보조 그룹 추가
usermod -a -G dba,oper,backupdba,dgdba,kmdba,racdba oracle

# 확인
id oracle
```

---

### 5. 커널 파라미터 및 limits 확인

커널 파라미터 적용 여부를 확인한다.

```bash
sysctl -p
```

limits 설정을 확인한다.

```bash
grep -v ^# /etc/security/limits.d/oracle-database-preinstall-19c.conf
```

---

### 6. 표준 디렉터리 생성 및 권한 설정

```bash
mkdir -p /u01/app/oracle/product/19.3.0/dbhome_1
mkdir -p /u01/app/oraInventory
chown -R oracle:oinstall /u01
chmod -R 775 /u01
```

디렉터리 구조는 아래와 같다.

```
/u01
 └─ app
    ├─ oracle
    │  └─ product
    │     └─ 19.3.0
    │        └─ dbhome_1   (ORACLE_HOME)
    └─ oraInventory
```

| 경로 | 설명 |
|------|------|
| ORACLE_BASE | /u01/app/oracle |
| ORACLE_HOME | /u01/app/oracle/product/19.3.0/dbhome_1 |
| oraInventory | 설치/패치/제거 메타데이터 저장 경로 |

---

### 7. oracle 계정 환경변수 설정

```bash
su - oracle
vi .bash_profile
```

아래 내용을 추가한다.

```bash
export ORACLE_BASE=/u01/app/oracle
export ORACLE_HOME=/u01/app/oracle/product/19.3.0/dbhome_1
export ORA_INVENTORY=/u01/app/oraInventory
export ORACLE_SID=ORA19
export PATH=$PATH:$HOME/bin:$ORACLE_HOME/bin
export LD_LIBRARY_PATH=$ORACLE_HOME/lib:/lib:/usr/lib
export CLASSPATH=$ORACLE_HOME/JRE:$ORACLE_HOME/jlib:$ORACLE_HOME/rdbms/jlib
```

환경변수를 적용하고 확인한다.

```bash
source .bash_profile
env | grep ORA
```

---

### 8. X11 Forwarding 설정 (GUI 설치 준비)

MobaXterm으로 oracle 유저 접속 후 xclock으로 GUI 동작을 확인한다.

```bash
echo $DISPLAY
xclock
```

xclock 명령어가 없으면 아래와 같이 설치한다.

```bash
yum install -y xorg-x11-apps
```

> X11 Forwarding은 원격 서버의 GUI 화면을 로컬에서 표시하는 기능이다.
> Oracle GUI 설치 프로그램(runInstaller) 실행 시 필요하다.

---

### 9. 설치 파일 업로드 및 압축 해제

<img width="993" height="619" alt="image" src="https://github.com/user-attachments/assets/481479f9-6c3c-4ee9-9d4c-d73e922f1c86" />

ORACLE_HOME 경로에서 설치 파일의 압축을 해제한다.

```bash
cd /u01/app/oracle/product/19.3.0/dbhome_1
unzip -q ~/LINUX.X64_193000_db_home.zip
```

> 설치 파일(LINUX.X64_193000_db_home.zip)은 Oracle 공식 사이트에서 다운로드한다.
> https://www.oracle.com/database/technologies/oracle-database-software-downloads.html

---

### 10. Oracle Software 설치 (GUI)

runInstaller를 실행하여 설치를 시작한다.

```bash
cd /u01/app/oracle/product/19.3.0/dbhome_1
./runInstaller
```

설치 옵션은 아래 순서대로 진행한다.

| 단계 | 설정값 |
|------|--------|
| 설치 유형 | Set Up Software Only |
| DB 유형 | Single instance database installation |
| 에디션 | Enterprise Edition |
| Oracle Base | /u01/app/oracle |
| Inventory 경로 | /u01/app/oraInventory (그룹: oinstall) |
| root 스크립트 | 자동 실행 또는 수동 실행 |

<img width="803" height="627" alt="image" src="https://github.com/user-attachments/assets/a797c051-fa73-4ac7-b268-fed19e3f6ff5" />

<img width="803" height="640" alt="image" src="https://github.com/user-attachments/assets/08da6da5-cec2-4c58-a6b1-0a650226ceb4" />

<img width="814" height="640" alt="image" src="https://github.com/user-attachments/assets/d2baba90-5265-4d79-b4bd-1041fc94aec9" />

<img width="795" height="642" alt="image" src="https://github.com/user-attachments/assets/99e6394a-7bd0-4f8d-96c6-e92bf7dc4de9" />

<img width="798" height="630" alt="image" src="https://github.com/user-attachments/assets/1946458a-0ed7-4901-9924-abad4f28b8a9" />

<img width="803" height="631" alt="image" src="https://github.com/user-attachments/assets/a139bae7-1176-4439-bddd-999bfba3119d" />

<img width="803" height="634" alt="image" src="https://github.com/user-attachments/assets/5ef5ddae-0d0f-4861-9a57-5118c6a42e3b" />

<img width="806" height="630" alt="image" src="https://github.com/user-attachments/assets/afa860cd-e1ad-442e-aa77-9a7151372549" />

---

### 11. Swap 부족 경고 해결

설치 중 Prerequisite Checks 단계에서 Swap 경고가 발생하면 아래와 같이 Swap을 추가한다.

<img width="960" height="761" alt="image" src="https://github.com/user-attachments/assets/4ed290ae-8f15-4bfa-ac1d-f2992dbfa8c9" />

Swap 상태를 먼저 확인한다.

```bash
free -h
swapon --show
```

<img width="851" height="141" alt="image" src="https://github.com/user-attachments/assets/41e56732-297f-4814-becd-9d17bba34f0a" />

Swap 파일을 추가한다 (예: 3GB).

```bash
# Swap 파일 생성
dd if=/dev/zero of=/swapfile bs=1G count=3

# 권한 설정
chmod 600 /swapfile

# Swap 포맷
mkswap /swapfile

# Swap 활성화
swapon /swapfile

# 재부팅 후에도 유지되도록 설정
echo '/swapfile swap swap defaults 0 0' >> /etc/fstab
```

적용 후 확인한다.

```bash
free -h
```

<img width="812" height="115" alt="image" src="https://github.com/user-attachments/assets/c5c02cf2-01bb-49e9-bbae-6ad59de11763" />

이후 설치를 다시 진행한다.

<img width="838" height="643" alt="image" src="https://github.com/user-attachments/assets/606c0cc7-cb3b-4c05-8a25-de7c8556394d" />

&nbsp;

완료 화면 예시:

<img width="798" height="638" alt="image" src="https://github.com/user-attachments/assets/613d09f5-faf7-4b4a-8f29-8bfd216beb97" />
